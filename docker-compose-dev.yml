name: scrum-project-manager-backend
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    volumes:
      - ./environment/data/mysql:/var/lib/mysql
      - ./environment/mysql/init:/docker-entrypoint-initdb.d/
    command: [
      '--character-set-server=utf8mb4',
      '--collation-server=utf8mb4_unicode_ci',
      '--skip-character-set-client-handshake'
    ]
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    healthcheck:
      test: mysqladmin ping -h 127.0.0.1 -u root --password=$$MYSQL_ROOT_PASSWORD
      interval: 3s
      timeout: 60s
      retries: 3
    networks:
      - scrum-project-manager-backend-network
  discovery-service:
    image: discovery-service
    container_name: discovery-service
    build:
      context: ./discovery-service
      dockerfile: Dockerfile.dev
    ports:
      - "8761:8761"
    volumes:
      - ./discovery-service/src:/home/devuser/app/src
      - ./discovery-service/target:/home/devuser/app/target
      - ./discovery-service/pom.xml:/home/devuser/app/pom.xml
      - ~/.m2:/home/devuser/.m2
    environment:
      SPRING_PROFILES_ACTIVE: dev
      EUREKA_USERNAME: dev
      EUREKA_PASSWORD: dev
    healthcheck:
      test: ["CMD-SHELL", "curl -s -u $$EUREKA_USERNAME:$$EUREKA_PASSWORD http://localhost:8761/actuator/health | jq -r .status | grep -q UP"]
      interval: 5s
      timeout: 60s
      retries: 3
      # start_period: 10s
    networks:
      - scrum-project-manager-backend-network
  api-gateway:
    image: api-gateway
    container_name: api-gateway
    build:
      context: ./api-gateway
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - ./api-gateway/src:/home/devuser/app/src
      - ./api-gateway/target:/home/devuser/app/target
      - ./api-gateway/pom.xml:/home/devuser/app/pom.xml
      - ~/.m2:/home/devuser/.m2
    environment:
      SPRING_PROFILES_ACTIVE: dev
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:8080/actuator/health | jq -r .status | grep -q UP"]
      interval: 5s
      timeout: 60s
      retries: 3
      # start_period: 10s
    networks:
      - scrum-project-manager-backend-network
    depends_on:
      discovery-service:
        condition: service_healthy
  identity-service:
    image: identity-service
    container_name: identity-service
    build:
      context: ./identity-service
      dockerfile: Dockerfile.dev
    ports:
      - "8081:8081"
    volumes:
      - ./identity-service/src:/home/devuser/app/src
      - ./identity-service/target:/home/devuser/app/target
      - ./identity-service/pom.xml:/home/devuser/app/pom.xml
      - ~/.m2:/home/devuser/.m2
    environment:
      SPRING_PROFILES_ACTIVE: dev
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:8081/actuator/health | jq -r .status | grep -q UP"]
      interval: 5s
      timeout: 60s
      retries: 3
      # start_period: 10s
    networks:
      - scrum-project-manager-backend-network
    depends_on:
      mysql:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
      api-gateway:
        condition: service_healthy

networks:
  scrum-project-manager-backend-network:
    name: scrum-project-manager-backend-network
    driver: bridge
